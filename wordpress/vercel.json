{
  "builds": [
    {
      "src": "*.php",
      "use": "@vercel/php",
      "config": {
        "buildCommand": "composer install --no-interaction && composer run-script --no-interaction --timeout=0 serve",
        "outputDirectory": 0
      }
    }
  ]
}
